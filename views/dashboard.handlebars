{{#if posts.length}}
<section>
    <h2>Your Posts</h2>
    <div>
        {{#each posts as |post|}}
            {{> post-info post}}
        {{/each}}
    </div>
</section>
{{/if}}
<button>
    <a href="/dashboard/add-post">Add Post</a>
</button>